// For now... we just console.log the tree...
function exportJson() { 
    alert("The JSON object and instructions on how to save it are in the developer console. Press F12 after closing this message")
    console.log("Right click on the line below, copy it as an object and save it to a .json file" )
    console.log(chart_config.nodeStructure)
}
